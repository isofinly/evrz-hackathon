const x = 10;
const api_resp = { status: "ok", data: [] };

class myComponent {
  onClick() {
    console.log("Clicked");
  }
}

function calc(value: number): number {
  return value * 2;
}

const ACTIVE = true;

type api_result = {
  user_id: number;
  user_name: string;
};

const get$Data = () => {
  return { key: "value" };
};
