{"start_line": 7, "code": "PrintingRow = (props: PrintingRowProps) => {\n    const { text, endIndent, printingInput, typingValue, status, textProps } = props\n\n    const isActive = status === \"active\"\n    const isPrinted = status === \"printed\"\n\n    const containerRef = useRef<HTMLDivElement>(null)\n    const rowRef = useRef<HTMLDivElement>(null)\n\n    const { typingFontSize } = useCurrentFont()\n\n    useEffect(() => {\n        rowRef.current?.scroll({\n            left: 0,\n            behavior: \"smooth\"\n        })\n    }, [isActive])\n\n    usePrintingRowHorizontalScroll({ rowRef, text, typingValue })\n\n    return (\n        <Grid\n            gap={4}\n            bgColor={isActive ? \"main.100\" : undefined}\n            _hover={{\n                bgColor: \"main.100\"\n            }}\n            px={4}\n            h=\"max-content\"\n            ref={containerRef}\n        >\n            <Box overflowX=\"auto\" overflow=\"hidden\" ref={rowRef} w=\"100%\">\n                <Box pos=\"relative\" w=\"max-content\">\n                    {isActive && printingInput}\n\n                    <Text\n                        w=\"max-content\"\n                        fontSize={typingFontSize + \"px\"}\n                        whiteSpace=\"pre\"\n                        color={isPrinted ? \"printingTextActive\" : \"printingTextDisabled\"}\n                        {...textProps}\n                    >\n                        {text + \" \".repeat(endIndent ?? 0)}\n                    </Text>\n                </Box>\n            </Box>\n        </Grid>\n    )\n}"}