Root = () => {
    const location = useLocation()

    return (
        {/* <REVIEW>
            Стили должны передаваться через props параметры style или className, к примеру       
                <Flex
                className={styles.root}
                style={hight: location.pathname === paths.typingCodePage ? "100vh" : "auto"}>
            </REVIEW>*/}
        <Flex
            direction="column"
            h={location.pathname === paths.typingCodePage ? "100vh" : "auto"}
            minH="100vh"
        >
            <Header />

            <Flex as="main" flex="1" overflow="hidden" py="1px">
                <Suspense
                    fallback={
                        <CodeLoading>
                            <CodeLoadingTitle />
                            <CodeLoadingProgress />
                        </CodeLoading>
                    }
                >
                    <Outlet />
                </Suspense>
            </Flex>

            <Footer />
        </Flex>
    )
}