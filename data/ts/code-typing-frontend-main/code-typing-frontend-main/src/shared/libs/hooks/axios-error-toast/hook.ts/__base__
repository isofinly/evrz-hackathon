


import { useToast, UseToastOptions } from "@chakra-ui/react"

import { ApiError } from "@shared/api"
import { AxiosError } from "axios"


const toastStyles: UseToastOptions = {
    colorScheme: "red",
    status: "error"
}

export const useAxiosErrorToast = () => <BODY useAxiosErrorToast>