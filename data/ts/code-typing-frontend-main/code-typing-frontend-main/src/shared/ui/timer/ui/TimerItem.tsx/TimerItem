TimerItem = ({ time, postfix, required = true }: TimerItemProps) => {
    return (
        (!!time || required) && (
            <Text fontSize="x-large">
                {time}
                <Text as="span" fontSize="large" color="primary.300">
                    {postfix}
                </Text>
            </Text>
        )
    )
}