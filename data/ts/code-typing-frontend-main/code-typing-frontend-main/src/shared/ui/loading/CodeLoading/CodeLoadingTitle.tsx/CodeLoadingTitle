CodeLoadingTitle = (props: CodeLoadingTitleProps) => {
    const { title = "Loading...", ...textProps } = props

    return (
        <Text fontSize="2xl" {...textProps}>
            {/* <REVIEW>{`>>`} нужно заменить на svg элемент</REVIEW> */}
            {`>>`} {title}
        </Text>
    )
}