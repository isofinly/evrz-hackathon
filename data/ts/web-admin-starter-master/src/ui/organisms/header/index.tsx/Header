Header = () => {
  return (
    <HeaderContainer>
      <Logo to="/" />
      <Nav data={[]} />
      <LogoutButton type="primary" icon="logout" onClick={() => logOut()}>
        Выйти
      </LogoutButton>
    </HeaderContainer>
  );
}