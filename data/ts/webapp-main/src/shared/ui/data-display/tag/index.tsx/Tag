Tag = ({
  className,
  color = 'gray',
  icon,
  children,
}: TagProps) => {
  return (
    <div className={clsx(styles.tag, className)} data-color={color}>
      {icon && <Icon name={icon} />}
      <span className={styles.text}>{children}</span>
    </div>
  )
}