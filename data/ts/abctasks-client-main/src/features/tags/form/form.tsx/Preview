Preview: React.FC<FieldProps> = (props) => {
	const { $form, } = props;

	const values = useUnit($form.$values);

	return (
		<Center className={styles.preview}>
			<TagLabel {...values} />
		</Center>
	);
}