

import { RouteInstance, querySync } from 'atomic-router';
import {
	Effect,
	Event,
	Store,
	createEvent,
	createStore,
	sample,
} from 'effector';

import { controls } from '@/shared/configs';

export interface CreateQueryModelParams<T> <BODY CreateQueryModelParams>

export interface QueryModel<T> <BODY QueryModel>

export const createQueryModel = <T>(
	params: CreateQueryModelParams<T>
): QueryModel<T> => <BODY createQueryModel>;