import { StandardResponse } from '@/shared/types';

import { instance, normalizeQuery } from '../request';

import {
	ActivateParams,
	AuthResponse,
	LoginParams,
	RegistrationParams,
	User
} from './types';

export const auth = async () => <BODY auth>;

export const login = async (body: LoginParams) => <BODY login>;

export const registration = async (body: RegistrationParams) => <BODY registration>;

export const activate = async (query: ActivateParams) => <BODY activate>;

export const logout = async () => <BODY logout>;