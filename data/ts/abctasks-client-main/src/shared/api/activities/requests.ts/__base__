import { PaginationResponse, StandardResponse } from '@/shared/types';

import { instance, normalizeQuery } from '../request';

import {
	Activity,
	ActivityAction,
	ActivitySphere,
	GetActivitiesInRoomParams
} from './types';

export const getAll = async ({
	roomId,
	...query
}: GetActivitiesInRoomParams) => <BODY getAll>;

export const getActions = async () => <BODY getActions>;

export const getSpheres = async () => <BODY getSpheres>;