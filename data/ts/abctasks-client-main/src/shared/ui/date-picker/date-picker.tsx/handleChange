handleChange: MUIDatePIckerProps<Dayjs>['onChange'] = (date) => {
			let newDate: string | null;

			try {
				newDate = date?.toDate().toJSON() ?? null;
			} catch {
				newDate = null;
			}

			onChange(newDate);
		}