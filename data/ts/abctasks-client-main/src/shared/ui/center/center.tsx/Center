Center: React.FC<React.PropsWithChildren<CenterProps>> = (
	props
) => {
	const { className, children, height = 'auto' } = props;

	const classes = cn(styles.center, styles[height], className);

	return <div className={classes}>{children}</div>;
}