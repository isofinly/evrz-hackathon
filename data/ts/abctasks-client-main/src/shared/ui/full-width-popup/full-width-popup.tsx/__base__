



import CloseIcon from '@mui/icons-material/Close';

import {
	Dialog,
	DialogActions,
	DialogContent,
	DialogTitle,
	DialogTitleProps,
	IconButton,
	Slide,
	SlideProps,
} from '@mui/material';
import cn from 'classnames';
import * as React from 'react';

import { BasePopupProps, VoidFunction } from '@/shared/types';

import styles from './full-width-popup.module.css';

export interface FullWidthPopupProps
	extends BasePopupProps,
		React.PropsWithChildren <BODY FullWidthPopupProps>

export const FullWidthPopup: React.FC<FullWidthPopupProps> = (props) => <BODY FullWidthPopup>;

const Transition = React.forwardRef(function Transition(
	props: NonNullable<SlideProps>,
	ref: React.Ref<unknown>
) {
	return <Slide direction='up' ref={ref} {...props} />;
});