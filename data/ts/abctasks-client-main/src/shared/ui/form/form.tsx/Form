Form: React.FC<PaperProps<'form'>> = (props) => {
	const { className, children, ...rest } = props;

	return (
		<Paper className={cn(styles.form, className)} {...rest} component='form'>
			{children}
		</Paper>
	);
}