


import {
	Button,
	Dialog,
	DialogActions,
	DialogContent,
	DialogContentText,
	DialogTitle,
} from '@mui/material';
import * as React from 'react';

import { BasePopupProps, CommonProps, VoidFunction } from '@/shared/types';

export interface ConfirmProps extends BasePopupProps, CommonProps <BODY ConfirmProps>

export const Confirm: React.FC<ConfirmProps> = (props) => <BODY Confirm>;