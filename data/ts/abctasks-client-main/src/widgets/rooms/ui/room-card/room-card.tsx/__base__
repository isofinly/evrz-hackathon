import * as React from 'react';

import {
	OpenRoom,
	OpenUpdateRoomFormMenuItem,
	RemoveRoomMenuItem
} from '@/features/rooms';
import { ExitRoomUserMenuItem } from '@/features/users';

import { TemplateRoomCard } from '@/entities/rooms';

import { Room } from '@/shared/api';
import { CommonProps } from '@/shared/types';
import { EditMenu } from '@/shared/ui';

export interface RoomCardProps extends CommonProps, Room <BODY RoomCardProps>

export const RoomCard: React.FC<RoomCardProps> = (props) => <BODY RoomCard>;

const CardMenu: React.FC<{ roomId: number; canChange?: boolean }> = (props) => <BODY CardMenu>;