Aside: React.FC<AsideProps> = (props) => {
	const { className, disableBorder, } = props;

	return (
		<div className={cn(styles.aside, className)}>
			<TasksProgress disableBorder={disableBorder} />
			<LastActivities disableBorder={disableBorder} />
		</div>
	);
}