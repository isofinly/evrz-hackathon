RoomsLayout: React.FC<RoomsLayoutProps> = (props) => {
	const { className, children, } = props;

	return (
		<Layout
			className={className}
			slots={{
				header: <RoomsHeader />,
			}}>
			{children}
		</Layout>
	);
}