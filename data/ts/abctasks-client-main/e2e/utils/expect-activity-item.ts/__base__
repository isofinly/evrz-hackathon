import { Locator, Page, expect } from '@playwright/test';

export type ActivityItemType = 'success' | 'error' | 'warning';

export interface ExpectActivityItemParams <BODY ExpectActivityItemParams>

const iconIdMap = {
	success: 'AddIcon',
	error: 'DeleteIcon',
	warning: 'EditIcon',
};

export const expectActivityItem = async (
	params: ExpectActivityItemParams
): Promise<Locator> => <BODY expectActivityItem>;